// ===================== JavaScript DOM Events- Event Listener =====================

// === .addEventListener("event", callFunction)

// === .removeEventListener("event", callFunction)

// let counter = 0;
// const elm = document.querySelector(".display");
// const func = (e) => {
//   elm.classList.toggle("success");
//   console.log(counter++);
//   if (counter === 3) {
//     elm.removeEventListener("click", func);
//   }
// };

// elm.addEventListener("click", func);

// ================================================================
